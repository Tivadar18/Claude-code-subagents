.PHONY: run

run:
@if [ -z "$(agent)" ]; then echo "Usage: make run agent=<AGENT_SLUG> [input=...]"; exit 1; fi
@echo "Invoke agent: $(agent)"
@echo "# Example: make run agent=icp-segmentor input=./data/crm_export.csv"
